name: sarradabet-codeql-config

queries:
  - uses: security-and-quality
  - uses: ./.github/codeql/extensions/csrf-csrf

query-filters:
  - exclude:
      id: js/missing-token-validation
  - exclude:
      id: javascript/missing-token-validation
